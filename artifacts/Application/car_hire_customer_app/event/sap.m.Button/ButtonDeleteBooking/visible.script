if (typeof booking_status === "undefined" || booking_status === null || booking_status === "") { return; }  

var formattedText;

if(booking_status === "BOOKED"){
    formattedText = true
}else{
    formattedText = false;
}

return formattedText;