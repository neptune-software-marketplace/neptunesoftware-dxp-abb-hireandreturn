App.to(PageRentalDetails);

var options = {
    parameters: {
        //"where": { "bookingId": modelSimpleForm.getData().id } // Optional
        "where" : JSON.stringify({"bookingId": modelSimpleForm.getData().id})// Optional  
    }
};

apiGetBookingSignatureRentStart(options);


SaveChangesButton.firePress();



